
doctype html
html
 head
   title #{title}
   link(rel='stylesheet', href='/css/style.css')
   meta(name="viewport" content="width=device-width, initial-scale=1")
 body
   header.header
     h1 #{title}
   div.profile
    div
      each img in l2.imgIst
        div.profile
          img(src=`/images/${img}`)

        div.profile-details
      

       
